instance:
    hostname: localhost
    instance-id: "${spring.application.name} : ${instanceId : ${random.value}}"
       
       
       
 ---This is key configuration for creating multiple instances on Eureka server